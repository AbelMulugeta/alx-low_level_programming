#include <stdio.h>
/**
  * main - ommits q  & s and prints every letter
  * Return: 0 
**/
int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		if (ch != 'q' && ch != 'e')
		putchar(c);

	}	

	putchar('\n');
	return (0);

}
